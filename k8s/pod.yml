apiVersion: v1
kind: Pod
metadata:
  name: efs-pod
  namespace: efs_ns
spec:
  containers:
  - name: app
    image: ubuntu
    command: ["sleep"]
    args: ["1000"]
    volumeMounts:
    - name: plzwrk
      mountPath: /data
  volumes:
  - name: plzwrk
    persistentVolumeClaim:
      claimName: efs-pvc
